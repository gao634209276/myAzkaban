select trans_id,
       user_mobile,
       province_id,
       city_id,
       net_id,
       pay_id,
       brand_id,
       provider_key,
       application,
       version,
       interface_id,
       interface_name,
       interface_type,
       result,
       fail_reson,
       response_code,
       query_time,
       change_type,
       cur_package_id,
       cur_package_name,
       cur_package_type,
       new_package_id,
       new_package_name,
       new_packaget_type,
       new_product_type,
       operate_type,
       commit_type,
       is_reback,
       effect_type,
       user_ip,
       location,
       wi,
       sourceid
  from t_ods_biz_intf_v3
  where dt='${hiveconf:strdate}' and application like '1110%';

