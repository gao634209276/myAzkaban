select pro_id,
       city_id,
       bss_number,
       referees_name,
       referees_phone,
       receive_type,
       bss_date,
       store_name,
       broadband_rate,
       fee_type,
       charges,
       store_receive,
       fee_open,
       fee_measurement,
       fee_poundage,
       fee_flat,
       fee_equipment,
       fee_yc,
       fee_receive,
       fee_deposit,
       pay_type,
       pay_state,
       pay_time,
       pay_money,
       pay_depart,
       pay_bssid,
       bss_state,
       installed_address,
       customer_name,
       card_type,
       card_address,
       contact_person,
       contact_phone,
       invoice_title,
       customer_massage,
       account_type,
       back_reason,
       back_time,
       channel_type,open_account
  from t_ods_shop_broadband
 where dt = '${hiveconf:strdate}';

