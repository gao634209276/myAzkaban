select trans_id,
       user_mobile,
       province_id,
       city_id,
       net_id,
       pay_id,
       brand_id,
       location,
       provider_key,
       application,
       version,
       interface_id,
       interface_name,
       interface_type,
       result,
       fail_reson,
       response_code,
       query_time,
       cur_package_id,
       cur_package_name,
       new_package_id,
       new_package_name,
       operate_type,
       commit_type,
       is_reback,
       effect_type,
       auto_id,
       business_hall,
       pay_login_name,
       pay_money_price,
       pay_money_act,
       remark4,
       remark5
  from t_ods_biz_provider
 where dt = '${hiveconf:strdate}';

